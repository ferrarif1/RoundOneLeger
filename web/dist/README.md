This placeholder keeps the dist/ directory under version control so the Go
build always has something to embed. Run `npm run build` inside `web/` to
replace the contents with the real production bundle.
